name: Deploy Otomatis

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /var/www/lt3
          git pull origin main
          docker compose up -d --build